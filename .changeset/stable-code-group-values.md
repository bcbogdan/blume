---
"blume": patch
---

Support JSON-string tabValues mappings in CodeGroup and Tabs to group direct fences under stable panel identities. Invalid mappings fall back safely and expose a diagnostic attribute. Multi-fence panels retain per-block copy buttons, and copying never reads hidden alternatives.

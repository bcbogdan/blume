---
"blume": patch
---

Serve content-mounted agent artifacts and JSON docs API aliases alongside root compatibility routes. Keep explicit MCP and well-known routes separate, and include the JSON docs API in ejected apps.

Preserve independent overlapping deployment and content mounts, and advertise mounted JSON API self/recovery links and the content homepage in API catalogs.

Use pages.json as the catalog's JSON API entry point to avoid extensionless-file collisions with API directories in static builds. Resolve ejected UI aliases relative to the config alongside data aliases.
